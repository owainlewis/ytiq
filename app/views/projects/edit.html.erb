<div class="mb-8">
  <h1 class="text-xl font-semibold tracking-tight text-zinc-900">Edit Project</h1>
  <p class="mt-1 text-sm text-zinc-500"><%= @project.title %></p>
</div>

<%= render "form", project: @project %>
